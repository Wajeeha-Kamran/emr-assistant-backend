.venv/
.env
__pycache__/
*.pyc
/storage/
models/
*.log
docs/evidence/synthetic/parts/
docs/evidence/consult_*.wav
docs/evidence/diarized_output.txt

# Throwaway experiments. Kept on disk because they record what was
# tried, but not part of the project's history — pytest already skips
# them via norecursedirs.
scratch/

# Scratch recordings written into the repository root by earlier test runs.
# Eleven were committed by accident. The one the success-rate script needs
# lives in docs/evidence/pipeline_clip.wav instead, byte for byte identical.
/test_audio_*.wav
